\documentclass{article}

\usepackage{pgf}
\usepackage{tikz}

\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{4pt}


\usetikzlibrary{arrows,automata}
\usepackage[latin1]{inputenc}

% http://www.texample.net/tikz/examples/state-machine/


\begin{document}
\begin{tikzpicture}[->,>=stealth',shorten >=1pt,auto,node distance=2.8cm,semithick]

  \node[initial,state,label={$\{r_0\}$}] (r0)         {$s_0$};
  \node[state,label={$\{r_1\}$}]         (r1) [above right of=r0]  {$s_1$};
  \node[state,label={$\{r_2\}$}]         (r2) [below right of=r0]  {$s_2$};
  \node[state,label={$\{r_3\}$}]         (r3) [below right of=r1]  {$s_3$};

  \path (r0) edge              node {$a_{01}$} (r1)
            edge              node {$a_{03}$} (r3)
        (r1) edge [loop right] node {$a_{11}$} (r1)
            edge              node {$a_{13}$} (r3)
        (r2) edge              node {$a_{20}$} (r0)
             edge [loop below]             node {$a_{22}$} (r2);

\draw[latex'-latex'] (r2) -- node {$a_{23}$} (r3);

\end{tikzpicture}

\end{document}
